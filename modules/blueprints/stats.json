{
  "name": "StatsModule",
  "version": "1.0.0",
  "category": "analytics",
  "moduleType": "telemetry",
  "premium": false,
  "inputSchema": {
    "type": "object",
    "properties": {
      "target": { "type": "string", "format": "address" },
      "metric": { "type": "string" },
      "value": { "type": "string", "pattern": "^\\d+$" }
    },
    "required": ["target", "metric", "value"],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "metricId": { "type": "string" },
      "total": { "type": "string" }
    }
  },
  "eventSchema": [
    {
      "name": "MetricRecorded",
      "parameters": [
        { "name": "target", "type": "address", "indexed": true },
        { "name": "metric", "type": "bytes32", "indexed": true },
        { "name": "value", "type": "uint256", "indexed": false }
      ]
    }
  ],
  "abiAssumptions": [
    "function record(address target, bytes32 metric, uint256 value) external",
    "function getMetric(address target, bytes32 metric) external view returns (uint256)"
  ],
  "exampleUsage": "StatsModule, MainHub liderlik tabloları için gün sonu metrikleri toplar.",
  "integrationNotes": "Cron işçisi MetricRecorded olaylarını okuyup BigQuery/Talent Protocol skor panolarına aktarır."
}

{
  "name": "ClaimModule",
  "version": "1.0.0",
  "category": "rewards",
  "moduleType": "distribution",
  "premium": false,
  "inputSchema": {
    "type": "object",
    "properties": {
      "proof": { "type": "string" },
      "amount": { "type": "string", "pattern": "^\\d+$" }
    },
    "required": ["proof"],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "claimId": { "type": "string" },
      "amount": { "type": "string" }
    }
  },
  "eventSchema": [
    {
      "name": "Claimed",
      "parameters": [
        { "name": "user", "type": "address", "indexed": true },
        { "name": "amount", "type": "uint256", "indexed": false },
        { "name": "proofHash", "type": "bytes32", "indexed": false }
      ]
    }
  ],
  "abiAssumptions": [
    "function claim(bytes calldata proof, uint256 amount) external",
    "function isClaimed(address user) external view returns (bool)"
  ],
  "exampleUsage": "Merkle tabanlı airdrop programı ClaimModule ile yürütülür.",
  "integrationNotes": "Talent Protocol kullanıcı doğrulamaları claim sırasında zorlanabilir; Self XYZ KYC kontrolleri opsiyonel."
}

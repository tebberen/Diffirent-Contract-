export const PROJECT_NAME = 'CeloModuleX';

export function initApp() {
  console.log(`${PROJECT_NAME} frontend hazır.`);
  console.log('Eski Jello referansları kaldırıldı, UI bootstrap adı uyumlu.');
}
